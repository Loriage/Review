import SwiftUI

struct LogsView: View {
    var body: some View {
        Text("Vue des fichiers journaux à implémenter.")
            .navigationTitle("Fichiers journaux")
            .navigationBarTitleDisplayMode(.inline)
    }
}
