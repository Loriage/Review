import SwiftUI

struct LogsView: View {
    var body: some View {
        Text("logs.view.todo")
            .navigationTitle("logs.view.title")
            .navigationBarTitleDisplayMode(.inline)
    }
}
